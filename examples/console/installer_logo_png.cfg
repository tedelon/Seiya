[Application]
name=Guess the Number
version=1.0
entry_point=guessnumber:main
# Test logo= option with PNG format (auto-converted to ICO)
logo=logo_png.png
# We need to set this to get a console:
console=true

[Python]
version=3.12.10
bitness=64
format=bundled

[Include]
packages=guessnumber

# This optional section adds a command which can be run from the Windows
# command prompt.
[Command guessnumber]
entry_point=guessnumber:main
